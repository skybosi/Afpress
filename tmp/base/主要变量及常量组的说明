主要变量及常量说明：
变量：
in：从文件获得的待处理字符。
canc：一个字符被去0或1后剩余位的长度。通过canc_list[]获取
left：超过或不足一个字符时，剩余位串。
clen：超过或不足一个字符时，剩余位串的长度。
len：由clen与in的长度(8)组成，是真正被处理的位串的长度，一般将其重新赋值给in，在len>13时，left可能代替新的in作为真正被处理的位串。
head：唯一标志位串，及标志头，通过残长canc从head[]中获取。
addlen：累加长，由canc通过addlen[]获取。
chose_way：通过canc从chose_wayl[]获取，确定还原模式的变量。
out：经过处理后的待写入的量。在压缩程序里直接写入文件；在解压程序里要经过chose_way验证后获取应该处理方式后，再写入文件。

常量组：
canc_list[]：存放每一个字符的残长的数组，为获取残长提供索引。
meta_cans[]：一个获取残数的数组，除残长0为1外，其他情况为1<<canc-1
addlen[]：唯一标志位串的长度与残数位串长之和。
head[]:位长等于相应addlen的值，由唯一标志位串与后补零组成。
matchhead[]：位长为最长唯一标志位串的位长，由唯一标志位串后补0，使之保证为6或7位。
gethead[]：位长为最长唯一标志位串的位长，将matchhead前的唯一标志位串相应置1，后部分保持不变。
chose_wayl[]：将head的前部分唯一标志位串置0，后部分的首位置1，其他剩余位置0。除残长0外(置1)，其他满足1<<(canc-1)

press ->压缩 唯一标志头位串越短越好，影响压缩效果的原因在于，残长分布
upress ->压缩 唯一标志头位串越短越好，影响压缩效果的原因在于，唯一标志头的长度分布