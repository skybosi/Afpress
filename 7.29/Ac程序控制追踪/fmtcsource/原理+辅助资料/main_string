#include"fmt_c_code.h"
int main(int argc, char **argv)
{
	int j = 4;
	int i = KEY_HEAD_COUNT;
	int next[50], pos = 0;
	char *file_buf[] = {
		"vhij#voidyclutihtdio.h>gt#dteyfinRW",
		"vhij#voiyclutihtdinto.h>gt#dteyfinRW",
		"vhij#void///*inublecluintdene PRW",
		"vhij/int//*inecfloatdio.h>#define PRW"
	};
	while (j--)
	{
		printf("%s \n", file_buf[j]);
		while (i--)
		{
			printf("%s ", key_head[i]);
			getnext(key_head[i], next);
			pos = kmp(file_buf[j], key_head[i]);
			if (pos > 0)
			{
				printf("breaking......\n");
				break;
			}
		}
		printf("%s %d\n", key_head[i], pos);
		printf("\n");
		i = KEY_HEAD_COUNT;
	}
	return 0;
}